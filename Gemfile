source 'https://rubygems.org'

ruby '>= 3.0'

# Генератор использует только stdlib (psych, erb, optparse, json, openssl).
# Внешних рантайм-зависимостей нет — это осознанно (правило кейса: открытый код,
# без проприетарных решений).

group :test do
  # minitest входит в stdlib Ruby, отдельная установка не требуется.
end
